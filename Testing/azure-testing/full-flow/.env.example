# Example environment variables for d:\FinalYearProject\Testing\azure-testing\full-flow

# Server
PORT=8081
# PUBLIC_URL should include protocol and host (used to generate TwiML and websocket URLs)
# e.g. https://abcd-1234.ngrok.io
PUBLIC_URL=https://example.ngrok.io

# Azure Speech (required if using Azure recognizer / Azure TTS)
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=

# Voice selection (optional)
VOICE_EN=en-IN-PrabhatNeural

# Transcripts storage (optional)
TRANSCRIPTS_FILE=./data/transcripts.json

# LLM (Groq) API key (optional; if empty simulated responses are used)
GROQ_API_KEY=

# ElevenLabs TTS API key (optional)
ELEVENLABS_API_KEY=